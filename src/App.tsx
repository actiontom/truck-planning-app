import React from 'react';
import Layout from './components/Layout'; // Import the Layout component

const App: React.FC = () => {
  return (
    <div className="App">
      <Layout />
    </div>
  );
};

export default App;
